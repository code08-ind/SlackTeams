import type { FC } from 'react';
import Colors from './Colors';
export interface Props {
    isSelected?: boolean;
    label: string;
    defaultColor?: Colors;
    accentColor?: Colors;
}
declare const Item: FC<Props>;
export default Item;
